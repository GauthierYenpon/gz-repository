version: 2

models:

    - name: sales
      description: tableau sales 
      tests:
        - unique:
            column_name: "(orders_id || '-' || products_id)"
      columns:
          - name: orders_id
            description: identifier of the order
            tests : 
            - not_null 

          - name: products_id
            description: identifier of the products
            tests:
              - not_null

          - name: qty

          - name: turnover
            description: count of turnover
            tests:
              - not_null

          - name: purchase_cost
            description: cost of the purchases
            tests:
              - not_null

          - name: margin
            description: marge 